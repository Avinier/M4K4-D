# RP-01 Gate Registrations

| Field | Value |
|---|---|
| Status | Paper-screen gates **v0.3** (P09 added and `ACT-01` results, 2026-09-25) after the v0.2 closure audit of 2026-09-13; physical gate registrations remain open. External rigid-body demand is complete, but structural, actuator-internal and CAD/hardware blockers prevent paper passage. Freeze before first scored run. |
| Authority | Gate definitions and registration-record fields: `risk-prototype-plan.md` §RP-01. This file holds the registered numeric versions. |
| Physical baseline | `dimensional-baseline.md` v1.10 + Layout 04: 104 × 150 × 115 mm head, 49.5 mm neck; nominal D/E mass ~370/464/588 g roll/pitch/yaw at M008=20 g, with ~578–603 g complete C2 sensitivity. M008 and all accepted physical masses remain `U`; recalculate per servo candidate |

Gates to register (from the plan):

- **RP01-G01 Safety** — bounded state for every injected stop/timeout/feedback-loss/restart case
- **RP01-G02 Three-axis Core** — roll, pitch, yaw each execute registered authored-motion cases under representative load
- **RP01-G03 Motion quality** — range, tracking, repeatability, reversal, overshoot, settling, noise, vibration, cross-axis
- **RP01-G04 Integration** — camera calibratable/usable, wiring survives endurance, safe unpowered state
- **RP01-G05 Buildability** — credible fabrication/assembly/calibration/service/sourcing path with complete cost range
- **RP01-G06 Evidence** — commands, state, current/voltage, faults, video correlatable per run

Each registration uses the plan's required fields: Gate ID, Metric, Threshold, Rationale, Conditions, Repetitions, Instrument, Freeze record (date + builder approval **before** scored data is inspected). Threshold changes after results create a new gate version with a documented reason and fresh test set — never an edit.

## Candidate motion-case matrix — not registered

This matrix prepares coverage from `storyboard.md`; it does **not** freeze a threshold. “Minimum viable” and “best case” remain authored design envelopes until the builder approves a versioned gate registration.

| Case | Storyboard input | Candidate gate coverage | What must eventually be measured |
|---|---|---|---|
| Representative head load and envelope | Layout 04 complete head **104 H × 150 W × 115 D mm**; nominal D/E tree **~370/464/588 g roll/pitch/yaw** at M008=20 g, complete sensitivity ~578–603 g for M008=10–35 g; selected Waveshare no-touch SKU 30493 display, Camera Module 3 Wide SC0874 and ESP32-S3-Zero C2 | G02–G06 | Before registration: substitute each servo candidate's mass/envelope, register the scored per-axis downstream tree, as-built/CAD CoM/inertia, M008 state, axis offsets, ballast revision, selected-component proxies and complete moving interconnect. The old 95 mm band, 250 g mass, generic `J≈0.001 kg·m²` and `~0.2 N·m` values are inadmissible. |
| Finish-system mass and scale | 60 × 60 mm representative PLA coupon using the complete nine-step finish process | G05, G06 | bare/finished mass after ≥24 h cure, coating g/cm², exact products/passes and visual `PASS/ITERATE` for texture/chip/detail scale before M019a is coated |
| PLA thermal/creep viability | M010–M012 representative structure under worst credible parked pitch/roll load at measured sealed-head temperature | G01, G03–G06 | preregistered dwell duration, load/moment, maximum in-dwell deflection, residual deformation and inspection method; failure selects the recorded PETG/ASA structural fallback rather than being waived |
| CAD access and mass-boundary closure | Full-width opaque rear window mask; display flashing path; yaw-plane demateable connector/cut plane; minimal real service seams | G04–G06 | optical/interior concealment, flash/service demonstration, M020/M900 weighability without cutting, service access and no unowned/double-counted mass rows |
| Depth challenge | Current 110–115 mm core depth versus approximately 90 mm blockout | G03–G05 | display/camera/roll-support/harness clearance, revised shell area/mass, pitch moment arm, service access and stiffness; choose depth before CAD freeze |
| Gimbal-centre and roll-support sensitivity | Concept A A0/A1/A2 axis-to-CoM points plus rear-cartridge, annular/perimeter and displaced-roll-axis layouts from `physics.md`/concept note | G02–G05 | gravity/hold torque over full travel, busy-minute RMS/current/temperature, unpowered direction, display/camera/yoke/ear clearance, bearing reactions/deflection and revised CoM after mechanism mass is added |
| Slow smooth pitch | `HM-02` sleep, 28–35° over 2–3 s | G02, G03, G04 | low-speed smoothness, hold load/noise, final settle, safe rest |
| Fast sharp pitch | `HM-03` wake and `HM-06` yes; Yes critical speed segment 26°/220 ms `MJ5` | G02, G03 | peak current, tracking, overshoot, repeated reversal, ≤1 s phrase feasibility |
| Wide smooth yaw | `HM-04` search, authored ±35° / ±50° | G02–G04 | usable range, cable clearance, camera coverage, sector settle |
| Fast sharp yaw | `HM-07` no, exactly 2 cycles/1.00 s or 2 cycles/0.95 s; stitched `MJ5` reversals | G02, G03 | launch, every reversal and settle; peak acceleration/current, loaded reversal hysteresis, hunting/ringing and final settle |
| Small correction and loaded hold | `HM-05` tracking, authored 4° / 2–4°, plus fixed-command load sweeps and hold dwells | G03, G04 | smallest repeatable move; external-angle versus applied-torque hysteresis at `±0.50·τ_peak,op`, candidate maximum branch separation ≤0.50° / ≤0.25°; reversal delay, settled error, external-angle/current hunting, chatter and camera disturbance |
| Combined orientation reversal | Coordinated yaw/pitch/roll reversal at representative neutral and tilted poses | G03, G04 | complete face-orientation error; per-axis contributions mapped at the tested pose rather than assuming an equal one-third lash allocation |
| Pitch micro-pulse | `HM-08` laugh, 2 pulses/0.80 s or 3 pulses in 0.68 s active window; `MJ5` pulse train | G03 | 7°/100 ms local reversal, candidate loaded-hysteresis budget, unintended vibration/noise |
| Medium-fast roll | `HM-09` wobble, authored ±8° / ±12°; stitched `MJ5` reversals | G02, G03 | current roll peak speed/acceleration, rounded reversal, cross-axis disturbance and settle |
| Curious yes combined load | `HM-11A`: hold `R+8°/+12°`; pitch uses the explicit storyboard extrema | G02–G04 | roll static gravity/holding torque during dynamic pitch, coupling, clearance and camera usability |
| Curious no combined load | `HM-11B`: hold `R+8°/+12°` and `P-2°/-4°`; yaw to ±13° / ±18° | G02–G04 | roll holding torque during dynamic yaw, coupling, clearance and camera usability |
| Multi-axis recoil | `HM-15` startle; 250/180 ms outbound only | G01–G04 | simultaneous three-axis load, freeze/ringing, separate slower recovery and interruption; not an individual-axis peak |
| Loaded pitch structural dynamics | Best laugh's 7°/100 ms reversal under representative mass | G03, G04 | candidate first loaded pitch mode ≥30 Hz minimum / ≥40 Hz unshaped best case, plus damping and commanded ring-down; otherwise validate shaping/slower motion |
| Loaded yaw/roll structural dynamics | Fast no and wobble under representative mass | G03, G04 | candidate first loaded mode ≥25 Hz / ≥30 Hz plus measured damping and commanded ring-down |
| Harness restoring load and live-signal endurance | Complete H1/H2/H3 candidate head harness through yaw/pitch/roll workspace, with the selected camera streaming and representative concurrent electrical load | G03–G06 | angle-dependent restoring torque, slow-motion distortion, clamp/connector motion, actual installed radii/torsion, strain relief, frame gaps/drops, capture/driver errors, visible corruption, reset/re-enumeration, milestone inspection and unpowered pull. Route must approach each joint near its axis; candidate milestone counts and the exact definition of one cycle must be frozen before scored use. |
| Busy-minute actuator duty | Preregistered realistic busiest one-minute sequence assembled from the storyboard | G02–G06 | transient torque/current at required speed kept separate from RMS/current/thermal acceptance; voltage sag and all actuator temperatures logged before selection |
| Encoder, bench-IMU and base-IMU separation | RP-01 servo encoders provide joint angle; a bench IMU is optional for modal/backlash evidence; a later locomotion IMU mounts on the base, not the head | G03, G04, G06 | no runtime RP-01 IMU mass or timing path; bench measurements identify their instrument/frame; base attitude is never reconstructed by subtracting commanded neck angles |
| Profile-law conformance | Every scored motion segment | G03, G06 | logged/exported trajectory identifies profile code and version; measured `ω/α/jerk` remain within the registered authored case and `Cᵥ≤2.0`, `Cₐ≤2π` rapid-profile envelope |
| Controlled cancel | `HM-18` from every motion class | G01, G03 | command acceptance, deceleration, stale-keyframe rejection, bounded hold |
| Unpowered/fault rest | `HM-00` plus power/controller/feedback fault cases | G01, G04 | measured passive motion/backdrive, supported rest versus bounded forward sleep-like settle, hard-stop/cable behavior and restart policy; gearbox ratio alone is not evidence |

Best-case failure alone cannot fail V1 if the later-approved minimum-viable three-axis case passes and remains perceptually coherent. Conversely, a minimum-viable label here is not permission to declare a pass without preregistered thresholds and scored evidence.

## Paper-screen gates — v0.2 draft, 2026-09-13

These are candidate-screen definitions, not scored RP01-G01…G06 outcomes. Candidate C01 is XC330-M288-T at direct 1:1; see [the evidence and numerical screen](actuator-screen-01.md). Proposed nominal voltage is 5 V; 3.7 V is a severe sensitivity endpoint, not an accepted or measured sag floor. The underlying **external rigid-body** demand peaks are pitch 0.095289, roll 0.055991, yaw 0.109917 N·m; RMS demands are 0.049164, 0.031431 and 0.024952 N·m respectively. These values omit actuator-internal acceleration demand.

Common registration fields: author/date = this v0.2 draft, 2026-09-13; builder freeze approval = pending; rationale/metric/threshold/conditions/instrument/repetitions = rows below. Numerical rules are proposed before physical scoring. Paper comparisons already observed here are exploratory findings, not retrospective registered passes. Any changed threshold gets a new version. Missing evidence returns OPEN, never PASS.

| ID / metric | Proposed threshold and rationale | Conditions / instrument / repetitions | Current C01 result |
|---|---|---|---|
| P01 Configuration and carried mass | Every moving item owned once; exact candidate housings, attachments, centres and inertia assumptions recorded. Zero unexplained mass substitutions. Prevent reuse of a demand model for another mechanism. | Layout-03 ledger and candidate drawing/CAD audit; inspect all owners once per configuration revision. Retain C2 10/20/35 g and balance grids. | Documented 23 g housing matches current model; installed CoM/inertia and attachment verification remain E/U. Conditional model reuse only. |
| P02 Supply and curve provenance | Declare nominal and minimum loaded servo-terminal voltage, configuration, current limits and applicable curve source. Resolve contradictory speeds. No undocumented curve scaling. | Manufacturer evidence and rail budget; every candidate/voltage pair. Later confirm with servo-terminal voltage logging during simultaneous motion. | OPEN: proposed 5 V; actual sag floor unknown; graph voltage not labelled; 65/81 rpm discrepancy. |
| P03 Transient torque-speed | Every signed trajectory point, including supported actuator-internal equivalent inertia/current demand, lies within motoring/braking capability at nominal and minimum voltage, including duration/current limits. Report minimum margin and controlling case; require nonnegative margin after recorded uncertainty allowances. Add no hidden generic factor. | All retained mechanical cases plus named combined cases and declared profile-freedom coverage, at 1 ms or verified equivalent; every case once per revision. Curve interpolation/calculation is the paper instrument. Keep demand-aligned bound separate. | External-load peak-point comparison encouraging; complete overlay OPEN because internal acceleration demand is unknown. A 3.7 V M288 yaw case FAILS the 63 rpm speed envelope because no-load speed is 59 rpm. |
| P04 RMS / continuous duty | Capability must meet pitch 0.049164, roll 0.031431, yaw 0.024952 N·m RMS for the documented duty, speed, ambient and mounting. Peak duration limits must also hold. A stall fraction alone cannot establish measured thermal passage. | Both BC-60/MV-60 schedules and all retained physical-law corners, nominal/minimum voltage; one paper review per condition. Manufacturer thermal data or explicitly graded estimate; later current/temperature logs through repeated duty. | All axes below manufacturer 0.186 N·m estimate at 5 V; ESTIMATE SCREEN ONLY. Enclosure/duty and low-voltage thermal evidence OPEN. |
| P05 Coverage and uncertainty | Each assumption labelled D/E/U; all discarded cases justified. Cable/friction probes must not be declared measured bounds; quantify or bound omitted cross-axis effects and actuator-internal inertia before claiming combined-load closure. | Audit model membership, physical-law and demand-aligned cases, curious yes/no/startle, profile envelope and internal inertia boundary once per revision using source calculation and case inventory. | External rigid-body calculation accepted as input. Internal actuator demand remains `U`; candidate-specific coverage OPEN. |
| P06 Decision and rig handoff | No unconditional paper pass while P01–P05, P07 or P08 have unresolved evidence. Any conditional rig candidate must name each unresolved item and a measurement to close it. No purchase, fabrication release or physical gate pass implied. | Review candidate record and register once per revision. Before scored rig data, freeze configuration, quantitative physical thresholds, instruments, repetitions and builder approval. | C01 remains a comparison candidate only; yaw supply issue and structural/internal-inertia blockers explicit; overall OPEN. |
| P07 Structural dynamics | The representative loaded structure must meet the storyboard modal targets, or a slower/shaped trajectory must be re-authored and re-screened. No torque-curve margin substitutes for stiffness/damping evidence. | Pre-build calculation/FEA as a screen; representative loaded tap test and commanded ring-down as physical evidence. Pitch ≥30 Hz minimum / ≥40 Hz unshaped best case; yaw/roll ≥25/30 Hz. | OPEN / HIGH RISK: simple screens place the current pitch frame near 6–9 Hz and roll saddle near 21 Hz. |
| P08 CAD and hardware consistency | Zero unintended moving-part/fastener collisions; hard stops outside registered usable travel with checked load path; real bearing/insert/servo SKUs fit; sign multipliers and physical balance-trim method recorded. | Source collision grid including fasteners, stop-load calculation/test, purchased-part drawings/trial fits and interface sign table; rerun for every affected geometry revision. | FAIL/OPEN: two retainer screws collide; stops equal usable limits; bearing is trial-only; yaw/roll sign mapping and balance trim are unresolved. |

### v0.3 draft addition and results, 2026-09-25

The working family is now `ACT-01` (`decision.md`): XC330-M181-T yaw through the Layout 04 1:1 spur pair, XC330-M288-T pitch and roll, on the regulated 5.0 V `PB-HEAD` rail. P01–P08 thresholds are unchanged. P09 is a new proposed row, so these paper gates become draft v0.3. Builder freeze approval is still pending. Evidence is in [actuator-screen-01.md §Paper-closure pass](actuator-screen-01.md#paper-closure-pass--v03-2026-09-25).

| ID / metric | Proposed threshold and rationale | Conditions / instrument / repetitions | Result for `ACT-01` |
|---|---|---|---|
| P09 Transmission lash outside the servo loop | Every mesh, coupling or linkage between a servo output and its head axis carries a stated lash allowance. The allowance plus the servo's own measured reversal must fit the complete-output hysteresis budget (`≤0.50°` minimum viable, `≤0.25°` best case, `storyboard.md`). An unpreloaded external mesh cannot be counted as zero. | Paper: lash allowance per mesh from the gear standard or the anti-backlash design. Physical: loaded reversal at the servo horn, then at the head axis (bench test B4), per axis, before the scored G03 run. | Scissor pinion added 2026-09-25 (see the P09 row below); measured lash OPEN. XC330 backlash is unpublished. Pitch and roll are direct-drive. |

| Gate | v0.2 result | v0.3 result for `ACT-01` |
|---|---|---|
| P01 | 23 g housing matches; CoM/inertia `E` | Unchanged. M181 and M288 share the 23 g, 20 × 34 × 26 mm housing, so the swap adds no mass. The installed-part check and the official ROBOTIS STEP check stay open (RP-06 CAD) |
| P02 | OPEN: sag floor, graph voltage, 65/81 rpm | **Paper-bounded.** Minimum servo-terminal voltage in normal operation is 4.74 V (`E`, regulated rail minus the 150 mV drop); 3.7 V is a fault case only. The graph is 5.0 V by consistency (`E`). The 65/81 rpm conflict does not control any axis. B2 and B5 confirm |
| P03 | Complete overlay OPEN; M288 yaw FAILS at 3.7 V | **Paper-bounded pass.** With internal inertia bounded at 0.11–0.25 g·cm² motor-side (`E`), every axis clears the moving curve at 4.74 V; yaw has the least margin (0.028 N·m, 15%). The M288 yaw failure no longer applies (yaw is M181). B1 replaces the bound |
| P04 | Estimate screen only | **Estimate screen passes with internal inertia:** 2.6× (pitch), 4.5× (roll), 3.1× (yaw) under ROBOTIS's 20%-of-stall figures. Thermal evidence (B3) OPEN |
| P05 | Internal demand `U` | Internal demand moves to a sourced `E` bound. Cable/friction probes are still not measured bounds |
| P06 | OPEN | OPEN, but the family is now a **conditional rig candidate**: open items are named with their measurements (B1–B5, P07, P08, P09) |
| P07 | OPEN / HIGH RISK | **Paper screen passes for the frame (updated 2026-09-25 after the head CAD revision).** Linear FEA of the P-frame (`RP-06-cad/head/layout-04/fea/`): pitch 51.4 N·m/rad, about 41 Hz with 0.000779 kg·m² (33 Hz at E = 2.3 GPa); roll mount 187.5 N·m/rad, about 84 Hz. The old frame gave 0.98 N·m/rad (about 6 Hz) and 17.9 N·m/rad (about 27 Hz) by the same method. The servo gear train, horn, coupling and bolted joints are not in the model (`U`), so the physical threshold remains OPEN until a loaded tap/ring-down test (B6) |
| P08 | FAIL/OPEN | **Paper items closed 2026-09-25; hardware fit open.** Screw collisions removed, with fasteners now in the motion grid (56 poses, 1,228 pairs, 0 hits). Stops are 3° past usable travel with Ø2 steel dowels, loads checked under the `BD-13` Current Limits. Bearing is 696-2Z. Sign multipliers are derived and recorded (`motion-signs.json`). The physical balance trim path is ear and rear-cover trim seats (±1.69 mm roll-Y, ±0.60 mm pitch-X). The XC330 STEP is verified as the official file. Open: purchased-part trial fits, stop impact, insert and screw engagement, measured trim |
| P09 | — | **Design closed on paper 2026-09-25:** the body-side yaw pinion is a scissor gear with 0.22 N·m preload, ≥ 1.5× the 0.130 N·m yaw peak. Measured lash (B4) OPEN |

For thermal rig registration, specify ambient/enclosure, sensor locations, current/temperature limits and a numerical equilibrium criterion before scoring; do not invent those limits from the one-minute calculation. For motion-quality registration, carry forward the existing loaded reversal and modal targets in the candidate motion-case matrix and establish remaining dwell, noise, settling and fault-response limits. A paper draft does not fill the physical approval record.

## Registered physical gates

*(none yet)*
